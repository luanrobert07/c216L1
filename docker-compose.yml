services:
  api:
    build: .
    container_name: api-alunos
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  tests:
    build: .
    container_name: testes-api-alunos
    command: pytest -v
